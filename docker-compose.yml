version: "3.9"
services:
  mongo:
    image: mongo:7
    container_name: adirai-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: adirai_connect
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:

